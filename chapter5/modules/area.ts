const PI = 3.14

export const circumferenceArea = (radius: number): number => {
    return PI * radius * radius
}

export const rectangleArea = (height: number, width: number): number => {
    return height * width
}